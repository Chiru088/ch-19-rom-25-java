class SocialMedia {
	static 
    public static void main(String[] huhfu) {
        String mediaNames[] = {
            "Facebook", "Instagram", "Twitter", "Snapchat", "WhatsApp",
            "YouTube", "Telegram", "LinkedIn", "Reddit", "Pinterest"
        };

        /*System.out.println("In SocialMedia, the popular platforms are:");
        System.out.println(mediaNames[0] + ", " + mediaNames[1] + ", " + mediaNames[2] + ", " + mediaNames[3] + ", " +
                           mediaNames[4] + ", " + mediaNames[5] + ", " + mediaNames[6] + ", " + mediaNames[7] + ", " +
                           mediaNames[8] + ", " + mediaNames[9]);*/
						   
						   
						 for(String mediaName : mediaNames){  
								 System.out.println(mediaNames)
							 }
				
						
    }
}
